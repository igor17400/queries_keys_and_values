# Codes

Runnable companions to the tutorial sections. One file per topic under
`topics/`, matching the slug of the HTML page it belongs to.

```bash
uv sync
uv run python topics/scaled_dot_product.py
```
