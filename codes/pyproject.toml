[project]
name = "codes"
version = "0.1.0"
description = "Code accompanying the Queries, Keys, and Values tutorial"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.12.0",
]
